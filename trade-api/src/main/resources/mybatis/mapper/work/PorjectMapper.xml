<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.converage.mapper.work.ProjectMapper">
    <!--<resultMap id="projectMap" type="Project">-->
        <!--<id property="id" column="id"></id>-->
        <!--<result property="projectName" column="project_name"></result>-->
    <!--</resultMap>-->

    <!--<insert id="insertProject" parameterType="com.converage.entity.work.Project">-->
      <!--INSERT INTO work_project (id,project_name,remark,create_by,create_time,if_valid) VALUES-->
      <!--(#{project.id},#{project.projectName},#{project.remark},#{project.createBy},#{project.createTime},#{project.ifValid})-->
    <!--</insert>-->
    <!---->
    <!--<select id="selectSubProjects" resultMap="projectMap">-->
        <!--select id,project_name from work_project-->
    <!--</select>-->
</mapper>
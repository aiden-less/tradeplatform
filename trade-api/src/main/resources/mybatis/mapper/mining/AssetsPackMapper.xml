<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.converage.mapper.mining.AssetsPackMapper">


    <update id="cancelUserAssetsPack">
      UPDATE user_assets_pack SET if_valid = FALSE WHERE if_valid = TRUE AND id = #{assetsPackId} AND user_id = #{userId}
    </update>
</mapper>